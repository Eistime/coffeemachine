<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Users management</title>
    <script type="text/javascript" src="./js/jquery-1.12.0.js"></script>
    <script src="./js/main.js"></script>
    <link rel="stylesheet" href="/bootstrap-3.3.6-dist/css/bootstrap.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/css/main.css">
  </head>
  <body>
    <div class="container">
      <table class="table">
        <thead>
          <tr>
            <th>
              id
            </th>
            <th>
              Username
            </th>
            <th>
              Actions
            </th>
          </tr>
        </thead>
        <tbody>
            <% users.each do |user| %>
              <tr>
                <td><%= user[:id] %></td>
                <td><a href="/user/<%= user[:id] %>"><%= user[:username] %></a></td>
                <td>
                  <form class="delete-form" style="display: inline" action="/users/delete/<%= user[:id] %>" method="post">
                    <button type="submit" class="delete_user btn btn-danger" /><i class=" glyphicon glyphicon-trash"></i></button>
                  </form>
                  <a href="/users/edit/<%= user[:id] %>" class="edit_user btn btn-success"><i class="glyphicon glyphicon-pencil"></i></a>
                </td>
              </tr>
            <% end %>
        </tbody>
      </table>
    </div>

  </body>
</html>
